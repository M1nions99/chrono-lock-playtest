# CHRONO STATION 개발·배포 규칙

- Unity 6000.3.23f1. 현재 실행 씬은 `Assets/ChronoStation/Scenes/ChronoStationDemo.unity`다.
- 처음 네 단계는 자기 능력을 학습하고 이후 사물 조작을 해금한다. 우클릭 자기, 좌클릭 사물, 휠/1~4 능력 선택, Q 전체 해제 조작을 유지한다.
- `Assets/ChronoLock`은 현재 게임에서 사용하는 공용 코드와 에셋을 포함하므로 폴더 이름만 보고 구버전으로 삭제하지 않는다.
- 사용자는 새 최종 실행판이 나올 때마다 구버전을 삭제하고 프로젝트와 실행판을 이 GitHub 저장소에 공유하도록 요청했다.
- 새 빌드의 관련 검증을 완료한 뒤 최신 실행 폴더를 교체한다. 원본 프로젝트 변경도 저장소에 반영하고 실행판 ZIP은 GitHub Releases에 게시한다.
- 새 ZIP의 업로드와 다운로드 무결성을 확인한 다음 이전 Release와 구버전 실행 폴더를 삭제한다. 빌드 실패 또는 업로드 실패 시 마지막 정상 배포판을 유지한다.
- 최신 배포판 하나만 유지하며 README의 고정 다운로드 링크를 유지한다. Git 소스 변경 이력, Blender 원본, 사용 중인 에셋은 보존한다.
- 실행판에는 exe, 데이터, 필요한 DLL을 함께 넣는다. `Library`, `Temp`, `Logs`, `UserSettings`, 인증정보 및 IDE 자동 생성 파일은 업로드하지 않는다.
- Windows에서 재귀 삭제 전 절대 경로 범위와 링크 여부를 확인한다. 실행 중인 사용자 게임이나 Unity를 강제로 종료하지 않는다.
