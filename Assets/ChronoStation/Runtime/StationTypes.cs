namespace ChronoStation
{
    public enum StationAbility { Accelerate, Slow, Stop, Rewind }
    public enum StationTarget { Self, Object }
    public enum StationDeviceKind { Rotor, Generator, MovingPlatform, Cargo, Growth }
    public enum StationPage { Title, HUD, Pause, Options, Controls, Completed }
}
